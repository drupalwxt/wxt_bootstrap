{#
/**
 * @file
 * Theme implementation for an 'inline webform' element.
 *
 * Available variables
 * - content: Rendered block to display
 *
 * @ingroup themeable
 */
#}
<figure class="gc-pg-hlpfl provisional well mrgn-bttm-0" aria-live="polite"> 
  <figcaption class="wb-inv">{{ "Page feedback"|t }}</figcaption> 
  {{ content }}
</figure>
